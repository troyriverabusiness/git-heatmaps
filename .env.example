# Server Configuration
PORT=3000

# GitLab Configuration
# Optional: Specify a custom GitLab instance URL (defaults to https://gitlab.com)
# GITLAB_BASE_URL=https://gitlab.example.com

# Cache Configuration
# Cache TTL in milliseconds (default: 86400000 = 24 hours)
# Recommendation: Keep at 24 hours since contributions only update once per day
CACHE_TTL_MS=86400000

# Maximum cache entries before LRU eviction kicks in (default: 10000)
# Memory usage estimate: ~5KB per entry, so 10000 entries ≈ 50MB
# Adjust based on expected concurrent users and available memory:
#   - Small deployment (<100 users): 1000
#   - Medium deployment (100-1000 users): 5000
#   - Large deployment (>1000 users): 10000+
CACHE_MAX_SIZE=10000
